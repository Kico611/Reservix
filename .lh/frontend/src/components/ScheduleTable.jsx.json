{
    "sourceFile": "frontend/src/components/ScheduleTable.jsx",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 39,
            "patches": [
                {
                    "date": 1761140928222,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1761141380249,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,6 +1,7 @@\n import React from \"react\";\r\n import { days } from \"../utils/dateUtils\";\r\n+import \"../styles/ScheduleTable.css\";\r\n \r\n export default function ScheduleTable({\r\n   weekSchedule,\r\n   dayTimes,\r\n"
                },
                {
                    "date": 1761141635108,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,7 +1,6 @@\n import React from \"react\";\r\n import { days } from \"../utils/dateUtils\";\r\n-import \"../styles/ScheduleTable.css\";\r\n \r\n export default function ScheduleTable({\r\n   weekSchedule,\r\n   dayTimes,\r\n"
                },
                {
                    "date": 1761141819033,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,7 +1,9 @@\n import React from \"react\";\r\n import { days } from \"../utils/dateUtils\";\r\n+import \"./ScheduleTable.css\";\r\n \r\n+\r\n export default function ScheduleTable({\r\n   weekSchedule,\r\n   dayTimes,\r\n   intervals,\r\n"
                },
                {
                    "date": 1761141828908,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,7 +1,7 @@\n import React from \"react\";\r\n import { days } from \"../utils/dateUtils\";\r\n-import \"./ScheduleTable.css\";\r\n+import \"../styles/ScheduleTable.css\";\r\n \r\n \r\n export default function ScheduleTable({\r\n   weekSchedule,\r\n"
                },
                {
                    "date": 1761143300684,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,7 +1,6 @@\n import React from \"react\";\r\n import { days } from \"../utils/dateUtils\";\r\n-import \"../styles/ScheduleTable.css\";\r\n \r\n \r\n export default function ScheduleTable({\r\n   weekSchedule,\r\n"
                },
                {
                    "date": 1761144442217,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,8 +1,7 @@\n import React from \"react\";\r\n import { days } from \"../utils/dateUtils\";\r\n \r\n-\r\n export default function ScheduleTable({\r\n   weekSchedule,\r\n   dayTimes,\r\n   intervals,\r\n@@ -14,9 +13,11 @@\n     return h * 60 + m;\r\n   };\r\n \r\n   const getTermCells = (dayShort) => {\r\n+    console.log(\"Generating cells for:\", dayShort);\r\n     if (!dayTimes[dayShort]) {\r\n+      console.log(\"No dayTimes set for\", dayShort);\r\n       return [\r\n         <td key=\"empty\" className=\"empty-cell\" colSpan={3}>\r\n           Postavite radno vrijeme\r\n         </td>,\r\n@@ -27,8 +28,10 @@\n     const dayTerms = weekSchedule[dayShort] || [];\r\n     const dayStart = dayTimes[dayShort].start;\r\n     const dayEnd = dayTimes[dayShort].end;\r\n \r\n+    console.log(\"Day terms:\", dayTerms, \"Start:\", dayStart, \"End:\", dayEnd);\r\n+\r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n       const intStartMin = timeToMinutes(intStart);\r\n@@ -45,8 +48,9 @@\n         return intStartMin >= odMin && intStartMin < doMin;\r\n       });\r\n \r\n       if (term) {\r\n+        console.log(\"Found booked term:\", term, \"at interval\", intStart);\r\n         let span = 0;\r\n         for (let j = i; j < intervals.length; j++) {\r\n           const sMin = timeToMinutes(intervals[j].time);\r\n           const odMin = timeToMinutes(term.od);\r\n@@ -56,10 +60,12 @@\n         }\r\n         if (span <= 0) span = 1;\r\n \r\n         let className = \"booked\";\r\n-        let handleClick = () =>\r\n+        let handleClick = () => {\r\n+          console.log(\"Clicked booked term:\", dayShort, intStart, term);\r\n           handleTermClick(dayShort, intStart, term, term.datum);\r\n+        };\r\n \r\n         if (term.status === \"otkazan\") {\r\n           className += \" cancelled\";\r\n           handleClick = null;\r\n@@ -80,19 +86,17 @@\n           </td>\r\n         );\r\n         i += span;\r\n       } else {\r\n+        const freeDatum = weekSchedule[dayShort]?.[0]?.datum;\r\n+        console.log(\"Free slot at interval:\", intStart, \"datum:\", freeDatum);\r\n         cells.push(\r\n           <td\r\n             key={`free-${i}`}\r\n-            onClick={() =>\r\n-              handleTermClick(\r\n-                dayShort,\r\n-                intStart,\r\n-                null,\r\n-                weekSchedule[dayShort]?.[0]?.datum\r\n-              )\r\n-            }\r\n+            onClick={() => {\r\n+              console.log(\"Clicked free slot:\", dayShort, intStart, freeDatum);\r\n+              handleTermClick(dayShort, intStart, null, freeDatum);\r\n+            }}\r\n           ></td>\r\n         );\r\n         i++;\r\n       }\r\n"
                },
                {
                    "date": 1761144793313,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,14 +6,33 @@\n   dayTimes,\r\n   intervals,\r\n   handleTermClick,\r\n   formatPhoneNumber,\r\n+  weekStart, // dodaj weekStart iz Home.jsx\r\n }) {\r\n   const timeToMinutes = (timeStr) => {\r\n     const [h, m] = timeStr.split(\":\").map(Number);\r\n     return h * 60 + m;\r\n   };\r\n \r\n+  // mapa dana → offset od ponedjeljka\r\n+  const dayShortToOffset = {\r\n+    Pon: 0,\r\n+    Uto: 1,\r\n+    Sri: 2,\r\n+    Čet: 3,\r\n+    Pet: 4,\r\n+    Sub: 5,\r\n+    Ned: 6,\r\n+  };\r\n+\r\n+  // dobiva datum za dan u tjednu\r\n+  const getDateForDay = (dayShort) => {\r\n+    const date = new Date(weekStart);\r\n+    date.setDate(date.getDate() + dayShortToOffset[dayShort]);\r\n+    return date;\r\n+  };\r\n+\r\n   const getTermCells = (dayShort) => {\r\n     console.log(\"Generating cells for:\", dayShort);\r\n     if (!dayTimes[dayShort]) {\r\n       console.log(\"No dayTimes set for\", dayShort);\r\n@@ -86,9 +105,9 @@\n           </td>\r\n         );\r\n         i += span;\r\n       } else {\r\n-        const freeDatum = weekSchedule[dayShort]?.[0]?.datum;\r\n+        const freeDatum = getDateForDay(dayShort);\r\n         console.log(\"Free slot at interval:\", intStart, \"datum:\", freeDatum);\r\n         cells.push(\r\n           <td\r\n             key={`free-${i}`}\r\n"
                },
                {
                    "date": 1761318072729,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,162 @@\n+import React from \"react\";\r\n+import { days } from \"../utils/dateUtils\";\r\n+\r\n+export default function ScheduleTable({\r\n+  weekSchedule,\r\n+  dayTimes,\r\n+  intervals,\r\n+  handleTermClick,\r\n+  formatPhoneNumber,\r\n+  weekStart, // weekStart iz Home.jsx\r\n+}) {\r\n+  const timeToMinutes = (timeStr) => {\r\n+    if (!timeStr || typeof timeStr !== \"string\") {\r\n+      console.warn(\"Invalid timeStr passed to timeToMinutes:\", timeStr);\r\n+      return 0;\r\n+    }\r\n+    const [h, m] = timeStr.split(\":\").map(Number);\r\n+    return h * 60 + m;\r\n+  };\r\n+\r\n+  const dayShortToOffset = {\r\n+    Pon: 0,\r\n+    Uto: 1,\r\n+    Sri: 2,\r\n+    Čet: 3,\r\n+    Pet: 4,\r\n+    Sub: 5,\r\n+    Ned: 6,\r\n+  };\r\n+\r\n+  const getDateForDay = (dayShort) => {\r\n+    let date = weekStart instanceof Date ? new Date(weekStart) : new Date(weekStart);\r\n+    if (isNaN(date)) {\r\n+      console.error(\"Invalid weekStart:\", weekStart);\r\n+      date = new Date(); // fallback\r\n+    }\r\n+    date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n+    console.log(`Calculated date for ${dayShort}:`, date);\r\n+    return date;\r\n+  };\r\n+\r\n+  const getTermCells = (dayShort) => {\r\n+    console.log(\"Generating cells for:\", dayShort);\r\n+    if (!dayTimes[dayShort]) {\r\n+      console.log(\"No dayTimes set for\", dayShort);\r\n+      return [\r\n+        <td key=\"empty\" className=\"empty-cell\" colSpan={3}>\r\n+          Postavite radno vrijeme\r\n+        </td>,\r\n+      ];\r\n+    }\r\n+\r\n+    const cells = [];\r\n+    const dayTerms = weekSchedule[dayShort] || [];\r\n+    const dayStart = dayTimes[dayShort].start;\r\n+    const dayEnd = dayTimes[dayShort].end;\r\n+\r\n+    console.log(\"Day terms:\", dayTerms, \"Start:\", dayStart, \"End:\", dayEnd);\r\n+\r\n+    let i = 0;\r\n+    while (i < intervals.length) {\r\n+      const intStart = intervals[i].time;\r\n+      const intStartMin = timeToMinutes(intStart);\r\n+\r\n+      if (intStart < dayStart || intStart >= dayEnd) {\r\n+        cells.push(<td key={`empty-${i}`}></td>);\r\n+        i++;\r\n+        continue;\r\n+      }\r\n+\r\n+      const term = dayTerms.find((t) => {\r\n+        if (!t.od || !t.do) return false;\r\n+        const odMin = timeToMinutes(t.od);\r\n+        const doMin = timeToMinutes(t.do);\r\n+        return intStartMin >= odMin && intStartMin < doMin;\r\n+      });\r\n+\r\n+      if (term) {\r\n+        console.log(\"Found booked term:\", term, \"at interval\", intStart);\r\n+        let span = 0;\r\n+        for (let j = i; j < intervals.length; j++) {\r\n+          const sMin = timeToMinutes(intervals[j].time);\r\n+          const odMin = timeToMinutes(term.od);\r\n+          const doMin = timeToMinutes(term.do);\r\n+          if (sMin >= odMin && sMin < doMin) span++;\r\n+          else break;\r\n+        }\r\n+        if (span <= 0) span = 1;\r\n+\r\n+        let className = \"booked\";\r\n+        let handleClick = () => {\r\n+          console.log(\"Clicked booked term:\", dayShort, intStart, term);\r\n+          const datum = term.datum instanceof Date && !isNaN(term.datum)\r\n+            ? term.datum\r\n+            : getDateForDay(dayShort);\r\n+          handleTermClick(dayShort, intStart, term, datum);\r\n+        };\r\n+\r\n+        if (term.status === \"otkazan\") {\r\n+          className += \" cancelled\";\r\n+          handleClick = null;\r\n+        }\r\n+\r\n+        cells.push(\r\n+          <td\r\n+            key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n+            className={className}\r\n+            colSpan={span}\r\n+            onClick={handleClick}\r\n+          >\r\n+            <div className=\"booked-label\">\r\n+              {term.status === \"otkazan\"\r\n+                ? \"Otkazan\"\r\n+                : formatPhoneNumber(term.rezervirao) || \"Nepoznat\"}\r\n+            </div>\r\n+          </td>\r\n+        );\r\n+        i += span;\r\n+      } else {\r\n+        const freeDatum = getDateForDay(dayShort);\r\n+        console.log(\"Free slot at interval:\", intStart, \"datum:\", freeDatum);\r\n+        cells.push(\r\n+          <td\r\n+            key={`free-${i}`}\r\n+            onClick={() => {\r\n+              console.log(\"Clicked free slot:\", dayShort, intStart, freeDatum);\r\n+              handleTermClick(dayShort, intStart, null, freeDatum);\r\n+            }}\r\n+          ></td>\r\n+        );\r\n+        i++;\r\n+      }\r\n+    }\r\n+\r\n+    return cells;\r\n+  };\r\n+\r\n+  return (\r\n+    <div className=\"table-container\">\r\n+      <table>\r\n+        <thead>\r\n+          <tr>\r\n+            <th></th>\r\n+            {intervals.map((it) => (\r\n+              <th key={it.time} className=\"hour-cell\">\r\n+                {it.label}\r\n+              </th>\r\n+            ))}\r\n+          </tr>\r\n+        </thead>\r\n+        <tbody>\r\n+          {days.map((day) => (\r\n+            <tr key={day}>\r\n+              <td className=\"day-cell\">{day}</td>\r\n+              {getTermCells(day)}\r\n+            </tr>\r\n+          ))}\r\n+        </tbody>\r\n+      </table>\r\n+    </div>\r\n+  );\r\n+}\r\n"
                },
                {
                    "date": 1761318367126,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -54,10 +54,8 @@\n     const dayTerms = weekSchedule[dayShort] || [];\r\n     const dayStart = dayTimes[dayShort].start;\r\n     const dayEnd = dayTimes[dayShort].end;\r\n \r\n-    console.log(\"Day terms:\", dayTerms, \"Start:\", dayStart, \"End:\", dayEnd);\r\n-\r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n       const intStartMin = timeToMinutes(intStart);\r\n@@ -159,155 +157,4 @@\n       </table>\r\n     </div>\r\n   );\r\n }\r\n-import React from \"react\";\r\n-import { days } from \"../utils/dateUtils\";\r\n-\r\n-export default function ScheduleTable({\r\n-  weekSchedule,\r\n-  dayTimes,\r\n-  intervals,\r\n-  handleTermClick,\r\n-  formatPhoneNumber,\r\n-  weekStart, // dodaj weekStart iz Home.jsx\r\n-}) {\r\n-  const timeToMinutes = (timeStr) => {\r\n-    const [h, m] = timeStr.split(\":\").map(Number);\r\n-    return h * 60 + m;\r\n-  };\r\n-\r\n-  // mapa dana → offset od ponedjeljka\r\n-  const dayShortToOffset = {\r\n-    Pon: 0,\r\n-    Uto: 1,\r\n-    Sri: 2,\r\n-    Čet: 3,\r\n-    Pet: 4,\r\n-    Sub: 5,\r\n-    Ned: 6,\r\n-  };\r\n-\r\n-  // dobiva datum za dan u tjednu\r\n-  const getDateForDay = (dayShort) => {\r\n-    const date = new Date(weekStart);\r\n-    date.setDate(date.getDate() + dayShortToOffset[dayShort]);\r\n-    return date;\r\n-  };\r\n-\r\n-  const getTermCells = (dayShort) => {\r\n-    console.log(\"Generating cells for:\", dayShort);\r\n-    if (!dayTimes[dayShort]) {\r\n-      console.log(\"No dayTimes set for\", dayShort);\r\n-      return [\r\n-        <td key=\"empty\" className=\"empty-cell\" colSpan={3}>\r\n-          Postavite radno vrijeme\r\n-        </td>,\r\n-      ];\r\n-    }\r\n-\r\n-    const cells = [];\r\n-    const dayTerms = weekSchedule[dayShort] || [];\r\n-    const dayStart = dayTimes[dayShort].start;\r\n-    const dayEnd = dayTimes[dayShort].end;\r\n-\r\n-    console.log(\"Day terms:\", dayTerms, \"Start:\", dayStart, \"End:\", dayEnd);\r\n-\r\n-    let i = 0;\r\n-    while (i < intervals.length) {\r\n-      const intStart = intervals[i].time;\r\n-      const intStartMin = timeToMinutes(intStart);\r\n-\r\n-      if (intStart < dayStart || intStart >= dayEnd) {\r\n-        cells.push(<td key={`empty-${i}`}></td>);\r\n-        i++;\r\n-        continue;\r\n-      }\r\n-\r\n-      const term = dayTerms.find((t) => {\r\n-        const odMin = timeToMinutes(t.od);\r\n-        const doMin = timeToMinutes(t.do);\r\n-        return intStartMin >= odMin && intStartMin < doMin;\r\n-      });\r\n-\r\n-      if (term) {\r\n-        console.log(\"Found booked term:\", term, \"at interval\", intStart);\r\n-        let span = 0;\r\n-        for (let j = i; j < intervals.length; j++) {\r\n-          const sMin = timeToMinutes(intervals[j].time);\r\n-          const odMin = timeToMinutes(term.od);\r\n-          const doMin = timeToMinutes(term.do);\r\n-          if (sMin >= odMin && sMin < doMin) span++;\r\n-          else break;\r\n-        }\r\n-        if (span <= 0) span = 1;\r\n-\r\n-        let className = \"booked\";\r\n-        let handleClick = () => {\r\n-          console.log(\"Clicked booked term:\", dayShort, intStart, term);\r\n-          handleTermClick(dayShort, intStart, term, term.datum);\r\n-        };\r\n-\r\n-        if (term.status === \"otkazan\") {\r\n-          className += \" cancelled\";\r\n-          handleClick = null;\r\n-        }\r\n-\r\n-        cells.push(\r\n-          <td\r\n-            key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n-            className={className}\r\n-            colSpan={span}\r\n-            onClick={handleClick}\r\n-          >\r\n-            <div className=\"booked-label\">\r\n-              {term.status === \"otkazan\"\r\n-                ? \"Otkazan\"\r\n-                : formatPhoneNumber(term.rezervirao) || \"Nepoznat\"}\r\n-            </div>\r\n-          </td>\r\n-        );\r\n-        i += span;\r\n-      } else {\r\n-        const freeDatum = getDateForDay(dayShort);\r\n-        console.log(\"Free slot at interval:\", intStart, \"datum:\", freeDatum);\r\n-        cells.push(\r\n-          <td\r\n-            key={`free-${i}`}\r\n-            onClick={() => {\r\n-              console.log(\"Clicked free slot:\", dayShort, intStart, freeDatum);\r\n-              handleTermClick(dayShort, intStart, null, freeDatum);\r\n-            }}\r\n-          ></td>\r\n-        );\r\n-        i++;\r\n-      }\r\n-    }\r\n-\r\n-    return cells;\r\n-  };\r\n-\r\n-  return (\r\n-    <div className=\"table-container\">\r\n-      <table>\r\n-        <thead>\r\n-          <tr>\r\n-            <th></th>\r\n-            {intervals.map((it) => (\r\n-              <th key={it.time} className=\"hour-cell\">\r\n-                {it.label}\r\n-              </th>\r\n-            ))}\r\n-          </tr>\r\n-        </thead>\r\n-        <tbody>\r\n-          {days.map((day) => (\r\n-            <tr key={day}>\r\n-              <td className=\"day-cell\">{day}</td>\r\n-              {getTermCells(day)}\r\n-            </tr>\r\n-          ))}\r\n-        </tbody>\r\n-      </table>\r\n-    </div>\r\n-  );\r\n-}\r\n"
                },
                {
                    "date": 1761318382051,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -34,9 +34,8 @@\n       console.error(\"Invalid weekStart:\", weekStart);\r\n       date = new Date(); // fallback\r\n     }\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n-    console.log(`Calculated date for ${dayShort}:`, date);\r\n     return date;\r\n   };\r\n \r\n   const getTermCells = (dayShort) => {\r\n"
                },
                {
                    "date": 1761318389577,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -10,9 +10,8 @@\n   weekStart, // weekStart iz Home.jsx\r\n }) {\r\n   const timeToMinutes = (timeStr) => {\r\n     if (!timeStr || typeof timeStr !== \"string\") {\r\n-      console.warn(\"Invalid timeStr passed to timeToMinutes:\", timeStr);\r\n       return 0;\r\n     }\r\n     const [h, m] = timeStr.split(\":\").map(Number);\r\n     return h * 60 + m;\r\n"
                },
                {
                    "date": 1761318405211,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -113,9 +113,8 @@\n         );\r\n         i += span;\r\n       } else {\r\n         const freeDatum = getDateForDay(dayShort);\r\n-        console.log(\"Free slot at interval:\", intStart, \"datum:\", freeDatum);\r\n         cells.push(\r\n           <td\r\n             key={`free-${i}`}\r\n             onClick={() => {\r\n"
                },
                {
                    "date": 1761319090402,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,14 +6,12 @@\n   dayTimes,\r\n   intervals,\r\n   handleTermClick,\r\n   formatPhoneNumber,\r\n-  weekStart, // weekStart iz Home.jsx\r\n+  weekStart,\r\n }) {\r\n   const timeToMinutes = (timeStr) => {\r\n-    if (!timeStr || typeof timeStr !== \"string\") {\r\n-      return 0;\r\n-    }\r\n+    if (!timeStr || typeof timeStr !== \"string\") return 0;\r\n     const [h, m] = timeStr.split(\":\").map(Number);\r\n     return h * 60 + m;\r\n   };\r\n \r\n@@ -28,22 +26,17 @@\n   };\r\n \r\n   const getDateForDay = (dayShort) => {\r\n     let date = weekStart instanceof Date ? new Date(weekStart) : new Date(weekStart);\r\n-    if (isNaN(date)) {\r\n-      console.error(\"Invalid weekStart:\", weekStart);\r\n-      date = new Date(); // fallback\r\n-    }\r\n+    if (isNaN(date)) date = new Date();\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n   const getTermCells = (dayShort) => {\r\n-    console.log(\"Generating cells for:\", dayShort);\r\n     if (!dayTimes[dayShort]) {\r\n-      console.log(\"No dayTimes set for\", dayShort);\r\n       return [\r\n-        <td key=\"empty\" className=\"empty-cell\" colSpan={3}>\r\n+        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n           Postavite radno vrijeme\r\n         </td>,\r\n       ];\r\n     }\r\n@@ -71,9 +64,8 @@\n         return intStartMin >= odMin && intStartMin < doMin;\r\n       });\r\n \r\n       if (term) {\r\n-        console.log(\"Found booked term:\", term, \"at interval\", intStart);\r\n         let span = 0;\r\n         for (let j = i; j < intervals.length; j++) {\r\n           const sMin = timeToMinutes(intervals[j].time);\r\n           const odMin = timeToMinutes(term.od);\r\n@@ -83,15 +75,9 @@\n         }\r\n         if (span <= 0) span = 1;\r\n \r\n         let className = \"booked\";\r\n-        let handleClick = () => {\r\n-          console.log(\"Clicked booked term:\", dayShort, intStart, term);\r\n-          const datum = term.datum instanceof Date && !isNaN(term.datum)\r\n-            ? term.datum\r\n-            : getDateForDay(dayShort);\r\n-          handleTermClick(dayShort, intStart, term, datum);\r\n-        };\r\n+        let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n \r\n         if (term.status === \"otkazan\") {\r\n           className += \" cancelled\";\r\n           handleClick = null;\r\n@@ -106,9 +92,9 @@\n           >\r\n             <div className=\"booked-label\">\r\n               {term.status === \"otkazan\"\r\n                 ? \"Otkazan\"\r\n-                : formatPhoneNumber(term.rezervirao) || \"Nepoznat\"}\r\n+                : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n             </div>\r\n           </td>\r\n         );\r\n         i += span;\r\n@@ -116,12 +102,9 @@\n         const freeDatum = getDateForDay(dayShort);\r\n         cells.push(\r\n           <td\r\n             key={`free-${i}`}\r\n-            onClick={() => {\r\n-              console.log(\"Clicked free slot:\", dayShort, intStart, freeDatum);\r\n-              handleTermClick(dayShort, intStart, null, freeDatum);\r\n-            }}\r\n+            onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n           ></td>\r\n         );\r\n         i++;\r\n       }\r\n"
                },
                {
                    "date": 1761321182963,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -63,8 +63,10 @@\n         const doMin = timeToMinutes(t.do);\r\n         return intStartMin >= odMin && intStartMin < doMin;\r\n       });\r\n \r\n+      const termDatum = getDateForDay(dayShort); // <-- uvijek datum iz tjedna\r\n+\r\n       if (term) {\r\n         let span = 0;\r\n         for (let j = i; j < intervals.length; j++) {\r\n           const sMin = timeToMinutes(intervals[j].time);\r\n@@ -75,9 +77,9 @@\n         }\r\n         if (span <= 0) span = 1;\r\n \r\n         let className = \"booked\";\r\n-        let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n+        let handleClick = () => handleTermClick(dayShort, intStart, term, termDatum);\r\n \r\n         if (term.status === \"otkazan\") {\r\n           className += \" cancelled\";\r\n           handleClick = null;\r\n@@ -98,13 +100,12 @@\n           </td>\r\n         );\r\n         i += span;\r\n       } else {\r\n-        const freeDatum = getDateForDay(dayShort);\r\n         cells.push(\r\n           <td\r\n             key={`free-${i}`}\r\n-            onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n+            onClick={() => handleTermClick(dayShort, intStart, null, termDatum)}\r\n           ></td>\r\n         );\r\n         i++;\r\n       }\r\n"
                },
                {
                    "date": 1761321357165,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,4 +1,5 @@\n+// ScheduleTable.jsx\r\n import React from \"react\";\r\n import { days } from \"../utils/dateUtils\";\r\n \r\n export default function ScheduleTable({\r\n@@ -25,10 +26,9 @@\n     Ned: 6,\r\n   };\r\n \r\n   const getDateForDay = (dayShort) => {\r\n-    let date = weekStart instanceof Date ? new Date(weekStart) : new Date(weekStart);\r\n-    if (isNaN(date)) date = new Date();\r\n+    const date = new Date(weekStart);\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n@@ -63,9 +63,9 @@\n         const doMin = timeToMinutes(t.do);\r\n         return intStartMin >= odMin && intStartMin < doMin;\r\n       });\r\n \r\n-      const termDatum = getDateForDay(dayShort); // <-- uvijek datum iz tjedna\r\n+      const termDatum = getDateForDay(dayShort); // TOČAN DATUM IZ TJEDNA\r\n \r\n       if (term) {\r\n         let span = 0;\r\n         for (let j = i; j < intervals.length; j++) {\r\n"
                },
                {
                    "date": 1761321366341,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,5 +1,4 @@\n-// ScheduleTable.jsx\r\n import React from \"react\";\r\n import { days } from \"../utils/dateUtils\";\r\n \r\n export default function ScheduleTable({\r\n@@ -26,9 +25,10 @@\n     Ned: 6,\r\n   };\r\n \r\n   const getDateForDay = (dayShort) => {\r\n-    const date = new Date(weekStart);\r\n+    let date = weekStart instanceof Date ? new Date(weekStart) : new Date(weekStart);\r\n+    if (isNaN(date)) date = new Date();\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n@@ -63,9 +63,9 @@\n         const doMin = timeToMinutes(t.do);\r\n         return intStartMin >= odMin && intStartMin < doMin;\r\n       });\r\n \r\n-      const termDatum = getDateForDay(dayShort); // TOČAN DATUM IZ TJEDNA\r\n+      const termDatum = getDateForDay(dayShort); // <-- uvijek datum iz tjedna\r\n \r\n       if (term) {\r\n         let span = 0;\r\n         for (let j = i; j < intervals.length; j++) {\r\n"
                },
                {
                    "date": 1761321486207,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -24,11 +24,11 @@\n     Sub: 5,\r\n     Ned: 6,\r\n   };\r\n \r\n+  // Dobij datum za dan u tjednu na temelju weekStart (ponedjeljak)\r\n   const getDateForDay = (dayShort) => {\r\n-    let date = weekStart instanceof Date ? new Date(weekStart) : new Date(weekStart);\r\n-    if (isNaN(date)) date = new Date();\r\n+    const date = new Date(weekStart);\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n@@ -63,10 +63,8 @@\n         const doMin = timeToMinutes(t.do);\r\n         return intStartMin >= odMin && intStartMin < doMin;\r\n       });\r\n \r\n-      const termDatum = getDateForDay(dayShort); // <-- uvijek datum iz tjedna\r\n-\r\n       if (term) {\r\n         let span = 0;\r\n         for (let j = i; j < intervals.length; j++) {\r\n           const sMin = timeToMinutes(intervals[j].time);\r\n@@ -77,9 +75,9 @@\n         }\r\n         if (span <= 0) span = 1;\r\n \r\n         let className = \"booked\";\r\n-        let handleClick = () => handleTermClick(dayShort, intStart, term, termDatum);\r\n+        let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n \r\n         if (term.status === \"otkazan\") {\r\n           className += \" cancelled\";\r\n           handleClick = null;\r\n@@ -100,12 +98,13 @@\n           </td>\r\n         );\r\n         i += span;\r\n       } else {\r\n+        const freeDatum = getDateForDay(dayShort);\r\n         cells.push(\r\n           <td\r\n             key={`free-${i}`}\r\n-            onClick={() => handleTermClick(dayShort, intStart, null, termDatum)}\r\n+            onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n           ></td>\r\n         );\r\n         i++;\r\n       }\r\n"
                },
                {
                    "date": 1761324034414,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -32,27 +32,22 @@\n     return date;\r\n   };\r\n \r\n   const getTermCells = (dayShort) => {\r\n-    if (!dayTimes[dayShort]) {\r\n-      return [\r\n-        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n-          Postavite radno vrijeme\r\n-        </td>,\r\n-      ];\r\n-    }\r\n+    // Ako nema radnog vremena, koristi default 07:00-15:00\r\n+    const dayStart = dayTimes[dayShort]?.start || \"07:00\";\r\n+    const dayEnd = dayTimes[dayShort]?.end || \"15:00\";\r\n \r\n     const cells = [];\r\n     const dayTerms = weekSchedule[dayShort] || [];\r\n-    const dayStart = dayTimes[dayShort].start;\r\n-    const dayEnd = dayTimes[dayShort].end;\r\n \r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n       const intStartMin = timeToMinutes(intStart);\r\n \r\n       if (intStart < dayStart || intStart >= dayEnd) {\r\n+        // prazna ćelija ako interval nije unutar radnog vremena\r\n         cells.push(<td key={`empty-${i}`}></td>);\r\n         i++;\r\n         continue;\r\n       }\r\n"
                },
                {
                    "date": 1761324057567,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -32,22 +32,27 @@\n     return date;\r\n   };\r\n \r\n   const getTermCells = (dayShort) => {\r\n-    // Ako nema radnog vremena, koristi default 07:00-15:00\r\n-    const dayStart = dayTimes[dayShort]?.start || \"07:00\";\r\n-    const dayEnd = dayTimes[dayShort]?.end || \"15:00\";\r\n+    if (!dayTimes[dayShort]) {\r\n+      return [\r\n+        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n+          Postavite radno vrijeme\r\n+        </td>,\r\n+      ];\r\n+    }\r\n \r\n     const cells = [];\r\n     const dayTerms = weekSchedule[dayShort] || [];\r\n+    const dayStart = dayTimes[dayShort].start;\r\n+    const dayEnd = dayTimes[dayShort].end;\r\n \r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n       const intStartMin = timeToMinutes(intStart);\r\n \r\n       if (intStart < dayStart || intStart >= dayEnd) {\r\n-        // prazna ćelija ako interval nije unutar radnog vremena\r\n         cells.push(<td key={`empty-${i}`}></td>);\r\n         i++;\r\n         continue;\r\n       }\r\n"
                },
                {
                    "date": 1761324119481,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -24,28 +24,32 @@\n     Sub: 5,\r\n     Ned: 6,\r\n   };\r\n \r\n-  // Dobij datum za dan u tjednu na temelju weekStart (ponedjeljak)\r\n   const getDateForDay = (dayShort) => {\r\n     const date = new Date(weekStart);\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n+  // Odredi ukupni min i max sati za cijeli tjedan\r\n+  const allTimes = Object.values(dayTimes)\r\n+    .filter(Boolean)\r\n+    .flatMap((dt) => [dt.start, dt.end]);\r\n+\r\n+  const globalMin = allTimes.length\r\n+    ? allTimes.reduce((min, t) => (timeToMinutes(t) < timeToMinutes(min) ? t : min))\r\n+    : \"07:00\";\r\n+  const globalMax = allTimes.length\r\n+    ? allTimes.reduce((max, t) => (timeToMinutes(t) > timeToMinutes(max) ? t : max))\r\n+    : \"15:00\";\r\n+\r\n   const getTermCells = (dayShort) => {\r\n-    if (!dayTimes[dayShort]) {\r\n-      return [\r\n-        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n-          Postavite radno vrijeme\r\n-        </td>,\r\n-      ];\r\n-    }\r\n+    const dayStart = dayTimes[dayShort]?.start || globalMin;\r\n+    const dayEnd = dayTimes[dayShort]?.end || globalMax;\r\n \r\n     const cells = [];\r\n     const dayTerms = weekSchedule[dayShort] || [];\r\n-    const dayStart = dayTimes[dayShort].start;\r\n-    const dayEnd = dayTimes[dayShort].end;\r\n \r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n"
                },
                {
                    "date": 1761324129826,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -24,32 +24,28 @@\n     Sub: 5,\r\n     Ned: 6,\r\n   };\r\n \r\n+  // Dobij datum za dan u tjednu na temelju weekStart (ponedjeljak)\r\n   const getDateForDay = (dayShort) => {\r\n     const date = new Date(weekStart);\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n-  // Odredi ukupni min i max sati za cijeli tjedan\r\n-  const allTimes = Object.values(dayTimes)\r\n-    .filter(Boolean)\r\n-    .flatMap((dt) => [dt.start, dt.end]);\r\n-\r\n-  const globalMin = allTimes.length\r\n-    ? allTimes.reduce((min, t) => (timeToMinutes(t) < timeToMinutes(min) ? t : min))\r\n-    : \"07:00\";\r\n-  const globalMax = allTimes.length\r\n-    ? allTimes.reduce((max, t) => (timeToMinutes(t) > timeToMinutes(max) ? t : max))\r\n-    : \"15:00\";\r\n-\r\n   const getTermCells = (dayShort) => {\r\n-    const dayStart = dayTimes[dayShort]?.start || globalMin;\r\n-    const dayEnd = dayTimes[dayShort]?.end || globalMax;\r\n+    if (!dayTimes[dayShort]) {\r\n+      return [\r\n+        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n+          Postavite radno vrijeme\r\n+        </td>,\r\n+      ];\r\n+    }\r\n \r\n     const cells = [];\r\n     const dayTerms = weekSchedule[dayShort] || [];\r\n+    const dayStart = dayTimes[dayShort].start;\r\n+    const dayEnd = dayTimes[dayShort].end;\r\n \r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n"
                },
                {
                    "date": 1761324245590,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -24,28 +24,39 @@\n     Sub: 5,\r\n     Ned: 6,\r\n   };\r\n \r\n-  // Dobij datum za dan u tjednu na temelju weekStart (ponedjeljak)\r\n   const getDateForDay = (dayShort) => {\r\n     const date = new Date(weekStart);\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n+  // Odredi globalni start i end za cijeli tjedan\r\n+  const allDayTimes = Object.values(dayTimes).filter(Boolean);\r\n+\r\n+  const globalStart =\r\n+    allDayTimes.length > 0\r\n+      ? allDayTimes.reduce(\r\n+          (min, dt) => (timeToMinutes(dt.start) < timeToMinutes(min) ? dt.start : min),\r\n+          allDayTimes[0].start\r\n+        )\r\n+      : \"07:00\";\r\n+\r\n+  const globalEnd =\r\n+    allDayTimes.length > 0\r\n+      ? allDayTimes.reduce(\r\n+          (max, dt) => (timeToMinutes(dt.end) > timeToMinutes(max) ? dt.end : max),\r\n+          allDayTimes[0].end\r\n+        )\r\n+      : \"17:00\";\r\n+\r\n   const getTermCells = (dayShort) => {\r\n-    if (!dayTimes[dayShort]) {\r\n-      return [\r\n-        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n-          Postavite radno vrijeme\r\n-        </td>,\r\n-      ];\r\n-    }\r\n+    const dayStart = dayTimes[dayShort]?.start || globalStart;\r\n+    const dayEnd = dayTimes[dayShort]?.end || globalEnd;\r\n \r\n     const cells = [];\r\n     const dayTerms = weekSchedule[dayShort] || [];\r\n-    const dayStart = dayTimes[dayShort].start;\r\n-    const dayEnd = dayTimes[dayShort].end;\r\n \r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n"
                },
                {
                    "date": 1761324362700,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -24,39 +24,28 @@\n     Sub: 5,\r\n     Ned: 6,\r\n   };\r\n \r\n+  // Dobij datum za dan u tjednu na temelju weekStart (ponedjeljak)\r\n   const getDateForDay = (dayShort) => {\r\n     const date = new Date(weekStart);\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n-  // Odredi globalni start i end za cijeli tjedan\r\n-  const allDayTimes = Object.values(dayTimes).filter(Boolean);\r\n-\r\n-  const globalStart =\r\n-    allDayTimes.length > 0\r\n-      ? allDayTimes.reduce(\r\n-          (min, dt) => (timeToMinutes(dt.start) < timeToMinutes(min) ? dt.start : min),\r\n-          allDayTimes[0].start\r\n-        )\r\n-      : \"07:00\";\r\n-\r\n-  const globalEnd =\r\n-    allDayTimes.length > 0\r\n-      ? allDayTimes.reduce(\r\n-          (max, dt) => (timeToMinutes(dt.end) > timeToMinutes(max) ? dt.end : max),\r\n-          allDayTimes[0].end\r\n-        )\r\n-      : \"17:00\";\r\n-\r\n   const getTermCells = (dayShort) => {\r\n-    const dayStart = dayTimes[dayShort]?.start || globalStart;\r\n-    const dayEnd = dayTimes[dayShort]?.end || globalEnd;\r\n+    if (!dayTimes[dayShort]) {\r\n+      return [\r\n+        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n+          Postavite radno vrijeme\r\n+        </td>,\r\n+      ];\r\n+    }\r\n \r\n     const cells = [];\r\n     const dayTerms = weekSchedule[dayShort] || [];\r\n+    const dayStart = dayTimes[dayShort].start;\r\n+    const dayEnd = dayTimes[dayShort].end;\r\n \r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n"
                },
                {
                    "date": 1761324695227,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -24,36 +24,30 @@\n     Sub: 5,\r\n     Ned: 6,\r\n   };\r\n \r\n-  // Dobij datum za dan u tjednu na temelju weekStart (ponedjeljak)\r\n   const getDateForDay = (dayShort) => {\r\n     const date = new Date(weekStart);\r\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n   const getTermCells = (dayShort) => {\r\n-    if (!dayTimes[dayShort]) {\r\n-      return [\r\n-        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n-          Postavite radno vrijeme\r\n-        </td>,\r\n-      ];\r\n-    }\r\n-\r\n     const cells = [];\r\n     const dayTerms = weekSchedule[dayShort] || [];\r\n-    const dayStart = dayTimes[dayShort].start;\r\n-    const dayEnd = dayTimes[dayShort].end;\r\n \r\n+    // Ako nema postavljenog radnog vremena, koristi default 07:00 - 17:00\r\n+    let dayStart = dayTimes[dayShort]?.start || \"07:00\";\r\n+    let dayEnd = dayTimes[dayShort]?.end || \"17:00\";\r\n+\r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n       const intStartMin = timeToMinutes(intStart);\r\n \r\n+      // Ako je izvan radnog vremena\r\n       if (intStart < dayStart || intStart >= dayEnd) {\r\n-        cells.push(<td key={`empty-${i}`}></td>);\r\n+        cells.push(<td key={`ooh-${i}`} className=\"out-of-hours\"></td>);\r\n         i++;\r\n         continue;\r\n       }\r\n \r\n"
                },
                {
                    "date": 1761324772962,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -34,12 +34,20 @@\n   const getTermCells = (dayShort) => {\r\n     const cells = [];\r\n     const dayTerms = weekSchedule[dayShort] || [];\r\n \r\n-    // Ako nema postavljenog radnog vremena, koristi default 07:00 - 17:00\r\n-    let dayStart = dayTimes[dayShort]?.start || \"07:00\";\r\n-    let dayEnd = dayTimes[dayShort]?.end || \"17:00\";\r\n+    // Ako nema postavljenog radnog vremena, vrati praznu ćeliju s porukom\r\n+    if (!dayTimes[dayShort]) {\r\n+      return [\r\n+        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n+          Nema postavljenog radnog vremena\r\n+        </td>,\r\n+      ];\r\n+    }\r\n \r\n+    const dayStart = dayTimes[dayShort].start;\r\n+    const dayEnd = dayTimes[dayShort].end;\r\n+\r\n     let i = 0;\r\n     while (i < intervals.length) {\r\n       const intStart = intervals[i].time;\r\n       const intStartMin = timeToMinutes(intStart);\r\n"
                },
                {
                    "date": 1761325036746,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -109,8 +109,9 @@\n           ></td>\r\n         );\r\n         i++;\r\n       }\r\n+      \r\n     }\r\n \r\n     return cells;\r\n   };\r\n"
                },
                {
                    "date": 1761325075602,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -101,17 +101,19 @@\n         );\r\n         i += span;\r\n       } else {\r\n         const freeDatum = getDateForDay(dayShort);\r\n+\r\n+        // Pauze / slobodni termini unutar radnog vremena\r\n         cells.push(\r\n           <td\r\n             key={`free-${i}`}\r\n+            className=\"break\"\r\n             onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n           ></td>\r\n         );\r\n         i++;\r\n       }\r\n-      \r\n     }\r\n \r\n     return cells;\r\n   };\r\n"
                },
                {
                    "date": 1761325135719,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,94 +31,94 @@\n     return date;\r\n   };\r\n \r\n   const getTermCells = (dayShort) => {\r\n-    const cells = [];\r\n-    const dayTerms = weekSchedule[dayShort] || [];\r\n+  const cells = [];\r\n+  const dayTerms = weekSchedule[dayShort] || [];\r\n \r\n-    // Ako nema postavljenog radnog vremena, vrati praznu ćeliju s porukom\r\n-    if (!dayTimes[dayShort]) {\r\n-      return [\r\n-        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n-          Nema postavljenog radnog vremena\r\n-        </td>,\r\n-      ];\r\n-    }\r\n+  if (!dayTimes[dayShort]) {\r\n+    return [\r\n+      <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n+        Nema postavljenog radnog vremena\r\n+      </td>,\r\n+    ];\r\n+  }\r\n \r\n-    const dayStart = dayTimes[dayShort].start;\r\n-    const dayEnd = dayTimes[dayShort].end;\r\n+  const dayStart = dayTimes[dayShort].start;\r\n+  const dayEnd = dayTimes[dayShort].end;\r\n+  const dayStartMin = timeToMinutes(dayStart);\r\n+  const dayEndMin = timeToMinutes(dayEnd);\r\n \r\n-    let i = 0;\r\n-    while (i < intervals.length) {\r\n-      const intStart = intervals[i].time;\r\n-      const intStartMin = timeToMinutes(intStart);\r\n+  let i = 0;\r\n+  while (i < intervals.length) {\r\n+    const intStart = intervals[i].time;\r\n+    const intStartMin = timeToMinutes(intStart);\r\n \r\n-      // Ako je izvan radnog vremena\r\n-      if (intStart < dayStart || intStart >= dayEnd) {\r\n-        cells.push(<td key={`ooh-${i}`} className=\"out-of-hours\"></td>);\r\n-        i++;\r\n-        continue;\r\n-      }\r\n+    // Ispravno provjera radnog vremena\r\n+    if (intStartMin < dayStartMin || intStartMin >= dayEndMin) {\r\n+      cells.push(<td key={`ooh-${i}`} className=\"out-of-hours\"></td>);\r\n+      i++;\r\n+      continue;\r\n+    }\r\n \r\n-      const term = dayTerms.find((t) => {\r\n-        if (!t.od || !t.do) return false;\r\n-        const odMin = timeToMinutes(t.od);\r\n-        const doMin = timeToMinutes(t.do);\r\n-        return intStartMin >= odMin && intStartMin < doMin;\r\n-      });\r\n+    const term = dayTerms.find((t) => {\r\n+      if (!t.od || !t.do) return false;\r\n+      const odMin = timeToMinutes(t.od);\r\n+      const doMin = timeToMinutes(t.do);\r\n+      return intStartMin >= odMin && intStartMin < doMin;\r\n+    });\r\n \r\n-      if (term) {\r\n-        let span = 0;\r\n-        for (let j = i; j < intervals.length; j++) {\r\n-          const sMin = timeToMinutes(intervals[j].time);\r\n-          const odMin = timeToMinutes(term.od);\r\n-          const doMin = timeToMinutes(term.do);\r\n-          if (sMin >= odMin && sMin < doMin) span++;\r\n-          else break;\r\n-        }\r\n-        if (span <= 0) span = 1;\r\n+    if (term) {\r\n+      let span = 0;\r\n+      for (let j = i; j < intervals.length; j++) {\r\n+        const sMin = timeToMinutes(intervals[j].time);\r\n+        const odMin = timeToMinutes(term.od);\r\n+        const doMin = timeToMinutes(term.do);\r\n+        if (sMin >= odMin && sMin < doMin) span++;\r\n+        else break;\r\n+      }\r\n+      if (span <= 0) span = 1;\r\n \r\n-        let className = \"booked\";\r\n-        let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n+      let className = \"booked\";\r\n+      let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n \r\n-        if (term.status === \"otkazan\") {\r\n-          className += \" cancelled\";\r\n-          handleClick = null;\r\n-        }\r\n+      if (term.status === \"otkazan\") {\r\n+        className += \" cancelled\";\r\n+        handleClick = null;\r\n+      }\r\n \r\n-        cells.push(\r\n-          <td\r\n-            key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n-            className={className}\r\n-            colSpan={span}\r\n-            onClick={handleClick}\r\n-          >\r\n-            <div className=\"booked-label\">\r\n-              {term.status === \"otkazan\"\r\n-                ? \"Otkazan\"\r\n-                : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n-            </div>\r\n-          </td>\r\n-        );\r\n-        i += span;\r\n-      } else {\r\n-        const freeDatum = getDateForDay(dayShort);\r\n-\r\n-        // Pauze / slobodni termini unutar radnog vremena\r\n-        cells.push(\r\n-          <td\r\n-            key={`free-${i}`}\r\n-            className=\"break\"\r\n-            onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n-          ></td>\r\n-        );\r\n-        i++;\r\n-      }\r\n+      cells.push(\r\n+        <td\r\n+          key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n+          className={className}\r\n+          colSpan={span}\r\n+          onClick={handleClick}\r\n+        >\r\n+          <div className=\"booked-label\">\r\n+            {term.status === \"otkazan\"\r\n+              ? \"Otkazan\"\r\n+              : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n+          </div>\r\n+        </td>\r\n+      );\r\n+      i += span;\r\n+    } else {\r\n+      const freeDatum = getDateForDay(dayShort);\r\n+      cells.push(\r\n+        <td\r\n+          key={`free-${i}`}\r\n+          className=\"break\"\r\n+          onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n+        ></td>\r\n+      );\r\n+      i++;\r\n     }\r\n+  }\r\n \r\n-    return cells;\r\n-  };\r\n+  return cells;\r\n+};\r\n \r\n+\r\n   return (\r\n     <div className=\"table-container\">\r\n       <table>\r\n         <thead>\r\n"
                },
                {
                    "date": 1761325208868,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -30,11 +30,13 @@\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n-  const getTermCells = (dayShort) => {\r\n+const getTermCells = (dayShort) => {\r\n   const cells = [];\r\n-  const dayTerms = weekSchedule[dayShort] || [];\r\n+  const dayTerms = (weekSchedule[dayShort] || []).sort(\r\n+    (a, b) => timeToMinutes(a.od) - timeToMinutes(b.od)\r\n+  );\r\n \r\n   if (!dayTimes[dayShort]) {\r\n     return [\r\n       <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n@@ -42,83 +44,71 @@\n       </td>,\r\n     ];\r\n   }\r\n \r\n-  const dayStart = dayTimes[dayShort].start;\r\n-  const dayEnd = dayTimes[dayShort].end;\r\n-  const dayStartMin = timeToMinutes(dayStart);\r\n-  const dayEndMin = timeToMinutes(dayEnd);\r\n+  const dayStartMin = timeToMinutes(dayTimes[dayShort].start);\r\n+  const dayEndMin = timeToMinutes(dayTimes[dayShort].end);\r\n \r\n   let i = 0;\r\n   while (i < intervals.length) {\r\n     const intStart = intervals[i].time;\r\n     const intStartMin = timeToMinutes(intStart);\r\n \r\n-    // Ispravno provjera radnog vremena\r\n+    // Izvan radnog vremena\r\n     if (intStartMin < dayStartMin || intStartMin >= dayEndMin) {\r\n       cells.push(<td key={`ooh-${i}`} className=\"out-of-hours\"></td>);\r\n       i++;\r\n       continue;\r\n     }\r\n \r\n-    const term = dayTerms.find((t) => {\r\n-      if (!t.od || !t.do) return false;\r\n-      const odMin = timeToMinutes(t.od);\r\n-      const doMin = timeToMinutes(t.do);\r\n-      return intStartMin >= odMin && intStartMin < doMin;\r\n-    });\r\n+    // Provjera zauzetog termina\r\n+    const term = dayTerms.find(\r\n+      (t) => intStartMin >= timeToMinutes(t.od) && intStartMin < timeToMinutes(t.do)\r\n+    );\r\n \r\n     if (term) {\r\n       let span = 0;\r\n       for (let j = i; j < intervals.length; j++) {\r\n         const sMin = timeToMinutes(intervals[j].time);\r\n-        const odMin = timeToMinutes(term.od);\r\n-        const doMin = timeToMinutes(term.do);\r\n-        if (sMin >= odMin && sMin < doMin) span++;\r\n+        if (sMin >= timeToMinutes(term.od) && sMin < timeToMinutes(term.do)) span++;\r\n         else break;\r\n       }\r\n-      if (span <= 0) span = 1;\r\n-\r\n       let className = \"booked\";\r\n       let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n-\r\n       if (term.status === \"otkazan\") {\r\n         className += \" cancelled\";\r\n         handleClick = null;\r\n       }\r\n-\r\n       cells.push(\r\n-        <td\r\n-          key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n-          className={className}\r\n-          colSpan={span}\r\n-          onClick={handleClick}\r\n-        >\r\n+        <td key={`term-${i}-${term.rezervirao || Math.random()}`} className={className} colSpan={span} onClick={handleClick}>\r\n           <div className=\"booked-label\">\r\n-            {term.status === \"otkazan\"\r\n-              ? \"Otkazan\"\r\n-              : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n+            {term.status === \"otkazan\" ? \"Otkazan\" : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n           </div>\r\n         </td>\r\n       );\r\n       i += span;\r\n     } else {\r\n-      const freeDatum = getDateForDay(dayShort);\r\n-      cells.push(\r\n-        <td\r\n-          key={`free-${i}`}\r\n-          className=\"break\"\r\n-          onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n-        ></td>\r\n-      );\r\n+      // Pauza između termina\r\n+      const nextTerm = dayTerms.find(t => intStartMin < timeToMinutes(t.od));\r\n+      if (nextTerm) {\r\n+        cells.push(\r\n+          <td key={`break-${i}`} className=\"break\" onClick={() => handleTermClick(dayShort, intStart, null, getDateForDay(dayShort))}></td>\r\n+        );\r\n+      } else {\r\n+        // Slobodni termini do kraja radnog vremena\r\n+        cells.push(\r\n+          <td key={`free-${i}`} onClick={() => handleTermClick(dayShort, intStart, null, getDateForDay(dayShort))}></td>\r\n+        );\r\n+      }\r\n       i++;\r\n     }\r\n   }\r\n \r\n   return cells;\r\n };\r\n \r\n \r\n+\r\n   return (\r\n     <div className=\"table-container\">\r\n       <table>\r\n         <thead>\r\n"
                },
                {
                    "date": 1761325235594,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -30,13 +30,11 @@\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n-const getTermCells = (dayShort) => {\r\n+  const getTermCells = (dayShort) => {\r\n   const cells = [];\r\n-  const dayTerms = (weekSchedule[dayShort] || []).sort(\r\n-    (a, b) => timeToMinutes(a.od) - timeToMinutes(b.od)\r\n-  );\r\n+  const dayTerms = weekSchedule[dayShort] || [];\r\n \r\n   if (!dayTimes[dayShort]) {\r\n     return [\r\n       <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n@@ -44,71 +42,83 @@\n       </td>,\r\n     ];\r\n   }\r\n \r\n-  const dayStartMin = timeToMinutes(dayTimes[dayShort].start);\r\n-  const dayEndMin = timeToMinutes(dayTimes[dayShort].end);\r\n+  const dayStart = dayTimes[dayShort].start;\r\n+  const dayEnd = dayTimes[dayShort].end;\r\n+  const dayStartMin = timeToMinutes(dayStart);\r\n+  const dayEndMin = timeToMinutes(dayEnd);\r\n \r\n   let i = 0;\r\n   while (i < intervals.length) {\r\n     const intStart = intervals[i].time;\r\n     const intStartMin = timeToMinutes(intStart);\r\n \r\n-    // Izvan radnog vremena\r\n+    // Ispravno provjera radnog vremena\r\n     if (intStartMin < dayStartMin || intStartMin >= dayEndMin) {\r\n       cells.push(<td key={`ooh-${i}`} className=\"out-of-hours\"></td>);\r\n       i++;\r\n       continue;\r\n     }\r\n \r\n-    // Provjera zauzetog termina\r\n-    const term = dayTerms.find(\r\n-      (t) => intStartMin >= timeToMinutes(t.od) && intStartMin < timeToMinutes(t.do)\r\n-    );\r\n+    const term = dayTerms.find((t) => {\r\n+      if (!t.od || !t.do) return false;\r\n+      const odMin = timeToMinutes(t.od);\r\n+      const doMin = timeToMinutes(t.do);\r\n+      return intStartMin >= odMin && intStartMin < doMin;\r\n+    });\r\n \r\n     if (term) {\r\n       let span = 0;\r\n       for (let j = i; j < intervals.length; j++) {\r\n         const sMin = timeToMinutes(intervals[j].time);\r\n-        if (sMin >= timeToMinutes(term.od) && sMin < timeToMinutes(term.do)) span++;\r\n+        const odMin = timeToMinutes(term.od);\r\n+        const doMin = timeToMinutes(term.do);\r\n+        if (sMin >= odMin && sMin < doMin) span++;\r\n         else break;\r\n       }\r\n+      if (span <= 0) span = 1;\r\n+\r\n       let className = \"booked\";\r\n       let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n+\r\n       if (term.status === \"otkazan\") {\r\n         className += \" cancelled\";\r\n         handleClick = null;\r\n       }\r\n+\r\n       cells.push(\r\n-        <td key={`term-${i}-${term.rezervirao || Math.random()}`} className={className} colSpan={span} onClick={handleClick}>\r\n+        <td\r\n+          key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n+          className={className}\r\n+          colSpan={span}\r\n+          onClick={handleClick}\r\n+        >\r\n           <div className=\"booked-label\">\r\n-            {term.status === \"otkazan\" ? \"Otkazan\" : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n+            {term.status === \"otkazan\"\r\n+              ? \"Otkazan\"\r\n+              : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n           </div>\r\n         </td>\r\n       );\r\n       i += span;\r\n     } else {\r\n-      // Pauza između termina\r\n-      const nextTerm = dayTerms.find(t => intStartMin < timeToMinutes(t.od));\r\n-      if (nextTerm) {\r\n-        cells.push(\r\n-          <td key={`break-${i}`} className=\"break\" onClick={() => handleTermClick(dayShort, intStart, null, getDateForDay(dayShort))}></td>\r\n-        );\r\n-      } else {\r\n-        // Slobodni termini do kraja radnog vremena\r\n-        cells.push(\r\n-          <td key={`free-${i}`} onClick={() => handleTermClick(dayShort, intStart, null, getDateForDay(dayShort))}></td>\r\n-        );\r\n-      }\r\n+      const freeDatum = getDateForDay(dayShort);\r\n+      cells.push(\r\n+        <td\r\n+          key={`free-${i}`}\r\n+          className=\"break\"\r\n+          onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n+        ></td>\r\n+      );\r\n       i++;\r\n     }\r\n   }\r\n \r\n   return cells;\r\n };\r\n \r\n \r\n-\r\n   return (\r\n     <div className=\"table-container\">\r\n       <table>\r\n         <thead>\r\n"
                },
                {
                    "date": 1761325241413,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,94 +31,92 @@\n     return date;\r\n   };\r\n \r\n   const getTermCells = (dayShort) => {\r\n-  const cells = [];\r\n-  const dayTerms = weekSchedule[dayShort] || [];\r\n+    const cells = [];\r\n+    const dayTerms = weekSchedule[dayShort] || [];\r\n \r\n-  if (!dayTimes[dayShort]) {\r\n-    return [\r\n-      <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n-        Nema postavljenog radnog vremena\r\n-      </td>,\r\n-    ];\r\n-  }\r\n+    // Ako nema postavljenog radnog vremena, vrati praznu ćeliju s porukom\r\n+    if (!dayTimes[dayShort]) {\r\n+      return [\r\n+        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n+          Nema postavljenog radnog vremena\r\n+        </td>,\r\n+      ];\r\n+    }\r\n \r\n-  const dayStart = dayTimes[dayShort].start;\r\n-  const dayEnd = dayTimes[dayShort].end;\r\n-  const dayStartMin = timeToMinutes(dayStart);\r\n-  const dayEndMin = timeToMinutes(dayEnd);\r\n+    const dayStart = dayTimes[dayShort].start;\r\n+    const dayEnd = dayTimes[dayShort].end;\r\n \r\n-  let i = 0;\r\n-  while (i < intervals.length) {\r\n-    const intStart = intervals[i].time;\r\n-    const intStartMin = timeToMinutes(intStart);\r\n+    let i = 0;\r\n+    while (i < intervals.length) {\r\n+      const intStart = intervals[i].time;\r\n+      const intStartMin = timeToMinutes(intStart);\r\n \r\n-    // Ispravno provjera radnog vremena\r\n-    if (intStartMin < dayStartMin || intStartMin >= dayEndMin) {\r\n-      cells.push(<td key={`ooh-${i}`} className=\"out-of-hours\"></td>);\r\n-      i++;\r\n-      continue;\r\n-    }\r\n+      // Ako je izvan radnog vremena\r\n+      if (intStart < dayStart || intStart >= dayEnd) {\r\n+        cells.push(<td key={`ooh-${i}`} className=\"out-of-hours\"></td>);\r\n+        i++;\r\n+        continue;\r\n+      }\r\n \r\n-    const term = dayTerms.find((t) => {\r\n-      if (!t.od || !t.do) return false;\r\n-      const odMin = timeToMinutes(t.od);\r\n-      const doMin = timeToMinutes(t.do);\r\n-      return intStartMin >= odMin && intStartMin < doMin;\r\n-    });\r\n+      const term = dayTerms.find((t) => {\r\n+        if (!t.od || !t.do) return false;\r\n+        const odMin = timeToMinutes(t.od);\r\n+        const doMin = timeToMinutes(t.do);\r\n+        return intStartMin >= odMin && intStartMin < doMin;\r\n+      });\r\n \r\n-    if (term) {\r\n-      let span = 0;\r\n-      for (let j = i; j < intervals.length; j++) {\r\n-        const sMin = timeToMinutes(intervals[j].time);\r\n-        const odMin = timeToMinutes(term.od);\r\n-        const doMin = timeToMinutes(term.do);\r\n-        if (sMin >= odMin && sMin < doMin) span++;\r\n-        else break;\r\n-      }\r\n-      if (span <= 0) span = 1;\r\n+      if (term) {\r\n+        let span = 0;\r\n+        for (let j = i; j < intervals.length; j++) {\r\n+          const sMin = timeToMinutes(intervals[j].time);\r\n+          const odMin = timeToMinutes(term.od);\r\n+          const doMin = timeToMinutes(term.do);\r\n+          if (sMin >= odMin && sMin < doMin) span++;\r\n+          else break;\r\n+        }\r\n+        if (span <= 0) span = 1;\r\n \r\n-      let className = \"booked\";\r\n-      let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n+        let className = \"booked\";\r\n+        let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n \r\n-      if (term.status === \"otkazan\") {\r\n-        className += \" cancelled\";\r\n-        handleClick = null;\r\n+        if (term.status === \"otkazan\") {\r\n+          className += \" cancelled\";\r\n+          handleClick = null;\r\n+        }\r\n+\r\n+        cells.push(\r\n+          <td\r\n+            key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n+            className={className}\r\n+            colSpan={span}\r\n+            onClick={handleClick}\r\n+          >\r\n+            <div className=\"booked-label\">\r\n+              {term.status === \"otkazan\"\r\n+                ? \"Otkazan\"\r\n+                : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n+            </div>\r\n+          </td>\r\n+        );\r\n+        i += span;\r\n+      } else {\r\n+        const freeDatum = getDateForDay(dayShort);\r\n+        cells.push(\r\n+          <td\r\n+            key={`free-${i}`}\r\n+            onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n+          ></td>\r\n+        );\r\n+        i++;\r\n       }\r\n-\r\n-      cells.push(\r\n-        <td\r\n-          key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n-          className={className}\r\n-          colSpan={span}\r\n-          onClick={handleClick}\r\n-        >\r\n-          <div className=\"booked-label\">\r\n-            {term.status === \"otkazan\"\r\n-              ? \"Otkazan\"\r\n-              : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n-          </div>\r\n-        </td>\r\n-      );\r\n-      i += span;\r\n-    } else {\r\n-      const freeDatum = getDateForDay(dayShort);\r\n-      cells.push(\r\n-        <td\r\n-          key={`free-${i}`}\r\n-          className=\"break\"\r\n-          onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n-        ></td>\r\n-      );\r\n-      i++;\r\n+      \r\n     }\r\n-  }\r\n \r\n-  return cells;\r\n-};\r\n+    return cells;\r\n+  };\r\n \r\n-\r\n   return (\r\n     <div className=\"table-container\">\r\n       <table>\r\n         <thead>\r\n"
                },
                {
                    "date": 1761325430094,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,92 +31,99 @@\n     return date;\r\n   };\r\n \r\n   const getTermCells = (dayShort) => {\r\n-    const cells = [];\r\n-    const dayTerms = weekSchedule[dayShort] || [];\r\n+  const cells = [];\r\n+  const dayTerms = (weekSchedule[dayShort] || []).sort(\r\n+    (a, b) => timeToMinutes(a.od) - timeToMinutes(b.od)\r\n+  );\r\n+  const breaks = (weekSchedule[dayShort]?.pauze || []).sort(\r\n+    (a, b) => timeToMinutes(a.od) - timeToMinutes(b.od)\r\n+  );\r\n \r\n-    // Ako nema postavljenog radnog vremena, vrati praznu ćeliju s porukom\r\n-    if (!dayTimes[dayShort]) {\r\n-      return [\r\n-        <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n-          Nema postavljenog radnog vremena\r\n-        </td>,\r\n-      ];\r\n-    }\r\n+  if (!dayTimes[dayShort]) {\r\n+    return [\r\n+      <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n+        Nema postavljenog radnog vremena\r\n+      </td>,\r\n+    ];\r\n+  }\r\n \r\n-    const dayStart = dayTimes[dayShort].start;\r\n-    const dayEnd = dayTimes[dayShort].end;\r\n+  const dayStartMin = timeToMinutes(dayTimes[dayShort].start);\r\n+  const dayEndMin = timeToMinutes(dayTimes[dayShort].end);\r\n \r\n-    let i = 0;\r\n-    while (i < intervals.length) {\r\n-      const intStart = intervals[i].time;\r\n-      const intStartMin = timeToMinutes(intStart);\r\n+  let i = 0;\r\n+  while (i < intervals.length) {\r\n+    const intStart = intervals[i].time;\r\n+    const intStartMin = timeToMinutes(intStart);\r\n \r\n-      // Ako je izvan radnog vremena\r\n-      if (intStart < dayStart || intStart >= dayEnd) {\r\n-        cells.push(<td key={`ooh-${i}`} className=\"out-of-hours\"></td>);\r\n-        i++;\r\n-        continue;\r\n-      }\r\n+    // Izvan radnog vremena\r\n+    if (intStartMin < dayStartMin || intStartMin >= dayEndMin) {\r\n+      cells.push(<td key={`ooh-${i}`} className=\"out-of-hours\"></td>);\r\n+      i++;\r\n+      continue;\r\n+    }\r\n \r\n-      const term = dayTerms.find((t) => {\r\n-        if (!t.od || !t.do) return false;\r\n-        const odMin = timeToMinutes(t.od);\r\n-        const doMin = timeToMinutes(t.do);\r\n-        return intStartMin >= odMin && intStartMin < doMin;\r\n-      });\r\n+    // Provjera zauzetog termina\r\n+    const term = dayTerms.find(\r\n+      (t) => intStartMin >= timeToMinutes(t.od) && intStartMin < timeToMinutes(t.do)\r\n+    );\r\n \r\n-      if (term) {\r\n-        let span = 0;\r\n-        for (let j = i; j < intervals.length; j++) {\r\n-          const sMin = timeToMinutes(intervals[j].time);\r\n-          const odMin = timeToMinutes(term.od);\r\n-          const doMin = timeToMinutes(term.do);\r\n-          if (sMin >= odMin && sMin < doMin) span++;\r\n-          else break;\r\n-        }\r\n-        if (span <= 0) span = 1;\r\n+    if (term) {\r\n+      let span = 0;\r\n+      for (let j = i; j < intervals.length; j++) {\r\n+        const sMin = timeToMinutes(intervals[j].time);\r\n+        if (sMin >= timeToMinutes(term.od) && sMin < timeToMinutes(term.do)) span++;\r\n+        else break;\r\n+      }\r\n+      let className = \"booked\";\r\n+      let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n+      if (term.status === \"otkazan\") {\r\n+        className += \" cancelled\";\r\n+        handleClick = null;\r\n+      }\r\n+      cells.push(\r\n+        <td key={`term-${i}-${term.rezervirao || Math.random()}`} className={className} colSpan={span} onClick={handleClick}>\r\n+          <div className=\"booked-label\">\r\n+            {term.status === \"otkazan\" ? \"Otkazan\" : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n+          </div>\r\n+        </td>\r\n+      );\r\n+      i += span;\r\n+      continue;\r\n+    }\r\n \r\n-        let className = \"booked\";\r\n-        let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n+    // Provjera pauze\r\n+    const pause = breaks.find(\r\n+      (p) => intStartMin >= timeToMinutes(p.od) && intStartMin < timeToMinutes(p.do)\r\n+    );\r\n \r\n-        if (term.status === \"otkazan\") {\r\n-          className += \" cancelled\";\r\n-          handleClick = null;\r\n-        }\r\n-\r\n-        cells.push(\r\n-          <td\r\n-            key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n-            className={className}\r\n-            colSpan={span}\r\n-            onClick={handleClick}\r\n-          >\r\n-            <div className=\"booked-label\">\r\n-              {term.status === \"otkazan\"\r\n-                ? \"Otkazan\"\r\n-                : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n-            </div>\r\n-          </td>\r\n-        );\r\n-        i += span;\r\n-      } else {\r\n-        const freeDatum = getDateForDay(dayShort);\r\n-        cells.push(\r\n-          <td\r\n-            key={`free-${i}`}\r\n-            onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}\r\n-          ></td>\r\n-        );\r\n-        i++;\r\n+    if (pause) {\r\n+      let span = 0;\r\n+      for (let j = i; j < intervals.length; j++) {\r\n+        const sMin = timeToMinutes(intervals[j].time);\r\n+        if (sMin >= timeToMinutes(pause.od) && sMin < timeToMinutes(pause.do)) span++;\r\n+        else break;\r\n       }\r\n-      \r\n+      cells.push(\r\n+        <td key={`pause-${i}`} className=\"break\" colSpan={span}></td>\r\n+      );\r\n+      i += span;\r\n+      continue;\r\n     }\r\n \r\n-    return cells;\r\n-  };\r\n+    // Slobodan termin\r\n+    const freeDatum = getDateForDay(dayShort);\r\n+    cells.push(\r\n+      <td key={`free-${i}`} onClick={() => handleTermClick(dayShort, intStart, null, freeDatum)}></td>\r\n+    );\r\n+    i++;\r\n+  }\r\n \r\n+  return cells;\r\n+};\r\n+\r\n+\r\n   return (\r\n     <div className=\"table-container\">\r\n       <table>\r\n         <thead>\r\n"
                },
                {
                    "date": 1761325577574,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -30,17 +30,17 @@\n     date.setDate(date.getDate() + (dayShortToOffset[dayShort] ?? 0));\r\n     return date;\r\n   };\r\n \r\n-  const getTermCells = (dayShort) => {\r\n+const getTermCells = (dayShort) => {\r\n   const cells = [];\r\n-  const dayTerms = (weekSchedule[dayShort] || []).sort(\r\n-    (a, b) => timeToMinutes(a.od) - timeToMinutes(b.od)\r\n-  );\r\n-  const breaks = (weekSchedule[dayShort]?.pauze || []).sort(\r\n-    (a, b) => timeToMinutes(a.od) - timeToMinutes(b.od)\r\n-  );\r\n \r\n+  // Pravilno dohvaćanje termina i pauza iz strukture weekSchedule\r\n+  const dayData = weekSchedule[dayShort] || {};\r\n+  const dayTerms = (dayData.termini || []).sort((a, b) => timeToMinutes(a.od) - timeToMinutes(b.od));\r\n+  const breaks = (dayData.pauze || []).sort((a, b) => timeToMinutes(a.od) - timeToMinutes(b.od));\r\n+\r\n+  // Ako nema postavljenog radnog vremena\r\n   if (!dayTimes[dayShort]) {\r\n     return [\r\n       <td key=\"empty\" className=\"empty-cell\" colSpan={intervals.length}>\r\n         Nema postavljenog radnog vremena\r\n@@ -74,16 +74,23 @@\n         const sMin = timeToMinutes(intervals[j].time);\r\n         if (sMin >= timeToMinutes(term.od) && sMin < timeToMinutes(term.do)) span++;\r\n         else break;\r\n       }\r\n+\r\n       let className = \"booked\";\r\n       let handleClick = () => handleTermClick(dayShort, intStart, term, term.datum);\r\n       if (term.status === \"otkazan\") {\r\n         className += \" cancelled\";\r\n         handleClick = null;\r\n       }\r\n+\r\n       cells.push(\r\n-        <td key={`term-${i}-${term.rezervirao || Math.random()}`} className={className} colSpan={span} onClick={handleClick}>\r\n+        <td\r\n+          key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n+          className={className}\r\n+          colSpan={span}\r\n+          onClick={handleClick}\r\n+        >\r\n           <div className=\"booked-label\">\r\n             {term.status === \"otkazan\" ? \"Otkazan\" : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n           </div>\r\n         </td>\r\n@@ -103,8 +110,9 @@\n         const sMin = timeToMinutes(intervals[j].time);\r\n         if (sMin >= timeToMinutes(pause.od) && sMin < timeToMinutes(pause.do)) span++;\r\n         else break;\r\n       }\r\n+\r\n       cells.push(\r\n         <td key={`pause-${i}`} className=\"break\" colSpan={span}></td>\r\n       );\r\n       i += span;\r\n"
                },
                {
                    "date": 1761329621546,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,8 +31,9 @@\n     return date;\r\n   };\r\n \r\n const getTermCells = (dayShort) => {\r\n+  console.log(\"TERM:\", term);\r\n   const cells = [];\r\n \r\n   // Pravilno dohvaćanje termina i pauza iz strukture weekSchedule\r\n   const dayData = weekSchedule[dayShort] || {};\r\n"
                },
                {
                    "date": 1761329629555,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,9 +31,8 @@\n     return date;\r\n   };\r\n \r\n const getTermCells = (dayShort) => {\r\n-  console.log(\"TERM:\", term);\r\n   const cells = [];\r\n \r\n   // Pravilno dohvaćanje termina i pauza iz strukture weekSchedule\r\n   const dayData = weekSchedule[dayShort] || {};\r\n"
                },
                {
                    "date": 1761329701364,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -68,8 +68,10 @@\n       (t) => intStartMin >= timeToMinutes(t.od) && intStartMin < timeToMinutes(t.do)\r\n     );\r\n \r\n     if (term) {\r\n+      console.log(\"TERM:\", term);\r\n+  console.log(\"term.rezervirao:\", term.rezervirao);\r\n       let span = 0;\r\n       for (let j = i; j < intervals.length; j++) {\r\n         const sMin = timeToMinutes(intervals[j].time);\r\n         if (sMin >= timeToMinutes(term.od) && sMin < timeToMinutes(term.do)) span++;\r\n"
                },
                {
                    "date": 1761329757831,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -68,10 +68,8 @@\n       (t) => intStartMin >= timeToMinutes(t.od) && intStartMin < timeToMinutes(t.do)\r\n     );\r\n \r\n     if (term) {\r\n-      console.log(\"TERM:\", term);\r\n-  console.log(\"term.rezervirao:\", term.rezervirao);\r\n       let span = 0;\r\n       for (let j = i; j < intervals.length; j++) {\r\n         const sMin = timeToMinutes(intervals[j].time);\r\n         if (sMin >= timeToMinutes(term.od) && sMin < timeToMinutes(term.do)) span++;\r\n"
                },
                {
                    "date": 1761329848216,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -90,10 +90,15 @@\n           colSpan={span}\r\n           onClick={handleClick}\r\n         >\r\n           <div className=\"booked-label\">\r\n-            {term.status === \"otkazan\" ? \"Otkazan\" : formatPhoneNumber?.(term.rezervirao) || \"Nepoznat\"}\r\n-          </div>\r\n+  {term.status === \"otkazan\"\r\n+    ? \"Otkazan\"\r\n+    : term.rezervirao !== undefined && term.rezervirao !== null\r\n+      ? (formatPhoneNumber?.(term.rezervirao) || term.rezervirao)\r\n+      : \"Nepoznat\"}\r\n+</div>\r\n+\r\n         </td>\r\n       );\r\n       i += span;\r\n       continue;\r\n"
                },
                {
                    "date": 1761329939748,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -92,13 +92,14 @@\n         >\r\n           <div className=\"booked-label\">\r\n   {term.status === \"otkazan\"\r\n     ? \"Otkazan\"\r\n-    : term.rezervirao !== undefined && term.rezervirao !== null\r\n+    : term.rezervirao\r\n       ? (formatPhoneNumber?.(term.rezervirao) || term.rezervirao)\r\n       : \"Nepoznat\"}\r\n </div>\r\n \r\n+\r\n         </td>\r\n       );\r\n       i += span;\r\n       continue;\r\n"
                }
            ],
            "date": 1761140928222,
            "name": "Commit-0",
            "content": "import React from \"react\";\r\nimport { days } from \"../utils/dateUtils\";\r\n\r\nexport default function ScheduleTable({\r\n  weekSchedule,\r\n  dayTimes,\r\n  intervals,\r\n  handleTermClick,\r\n  formatPhoneNumber,\r\n}) {\r\n  const timeToMinutes = (timeStr) => {\r\n    const [h, m] = timeStr.split(\":\").map(Number);\r\n    return h * 60 + m;\r\n  };\r\n\r\n  const getTermCells = (dayShort) => {\r\n    if (!dayTimes[dayShort]) {\r\n      return [\r\n        <td key=\"empty\" className=\"empty-cell\" colSpan={3}>\r\n          Postavite radno vrijeme\r\n        </td>,\r\n      ];\r\n    }\r\n\r\n    const cells = [];\r\n    const dayTerms = weekSchedule[dayShort] || [];\r\n    const dayStart = dayTimes[dayShort].start;\r\n    const dayEnd = dayTimes[dayShort].end;\r\n\r\n    let i = 0;\r\n    while (i < intervals.length) {\r\n      const intStart = intervals[i].time;\r\n      const intStartMin = timeToMinutes(intStart);\r\n\r\n      if (intStart < dayStart || intStart >= dayEnd) {\r\n        cells.push(<td key={`empty-${i}`}></td>);\r\n        i++;\r\n        continue;\r\n      }\r\n\r\n      const term = dayTerms.find((t) => {\r\n        const odMin = timeToMinutes(t.od);\r\n        const doMin = timeToMinutes(t.do);\r\n        return intStartMin >= odMin && intStartMin < doMin;\r\n      });\r\n\r\n      if (term) {\r\n        let span = 0;\r\n        for (let j = i; j < intervals.length; j++) {\r\n          const sMin = timeToMinutes(intervals[j].time);\r\n          const odMin = timeToMinutes(term.od);\r\n          const doMin = timeToMinutes(term.do);\r\n          if (sMin >= odMin && sMin < doMin) span++;\r\n          else break;\r\n        }\r\n        if (span <= 0) span = 1;\r\n\r\n        let className = \"booked\";\r\n        let handleClick = () =>\r\n          handleTermClick(dayShort, intStart, term, term.datum);\r\n\r\n        if (term.status === \"otkazan\") {\r\n          className += \" cancelled\";\r\n          handleClick = null;\r\n        }\r\n\r\n        cells.push(\r\n          <td\r\n            key={`term-${i}-${term.rezervirao || Math.random()}`}\r\n            className={className}\r\n            colSpan={span}\r\n            onClick={handleClick}\r\n          >\r\n            <div className=\"booked-label\">\r\n              {term.status === \"otkazan\"\r\n                ? \"Otkazan\"\r\n                : formatPhoneNumber(term.rezervirao) || \"Nepoznat\"}\r\n            </div>\r\n          </td>\r\n        );\r\n        i += span;\r\n      } else {\r\n        cells.push(\r\n          <td\r\n            key={`free-${i}`}\r\n            onClick={() =>\r\n              handleTermClick(\r\n                dayShort,\r\n                intStart,\r\n                null,\r\n                weekSchedule[dayShort]?.[0]?.datum\r\n              )\r\n            }\r\n          ></td>\r\n        );\r\n        i++;\r\n      }\r\n    }\r\n\r\n    return cells;\r\n  };\r\n\r\n  return (\r\n    <div className=\"table-container\">\r\n      <table>\r\n        <thead>\r\n          <tr>\r\n            <th></th>\r\n            {intervals.map((it) => (\r\n              <th key={it.time} className=\"hour-cell\">\r\n                {it.label}\r\n              </th>\r\n            ))}\r\n          </tr>\r\n        </thead>\r\n        <tbody>\r\n          {days.map((day) => (\r\n            <tr key={day}>\r\n              <td className=\"day-cell\">{day}</td>\r\n              {getTermCells(day)}\r\n            </tr>\r\n          ))}\r\n        </tbody>\r\n      </table>\r\n    </div>\r\n  );\r\n}\r\n"
        }
    ]
}